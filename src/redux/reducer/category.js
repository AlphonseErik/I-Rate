let initialState = {
  categoryList: [],
  productByCategory: [],
  selectedCategory: null,
};

export default (state = initialState, {type, payload}) => {
  switch (type) {
    
    default:
      return state;
  }
};
